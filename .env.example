# Mercado Pago — credenciais do vendedor.
# Nunca commitar o valor real: .gitignore já ignora .env*
MP_ACCESS_TOKEN=

# URL pública da loja, usada nas back_urls do checkout.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
